{
  "name": "",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "generate-lua-docs": "tsx ../scripts/generate-docs.ts",
    "predev": "mkdir -p src/content/docs/references && cp ../docs/* src/content/docs/references/",
    "dev": "astro dev",
    "prebuild": "mkdir -p src/content/docs/references && cp ../docs/* src/content/docs/references/",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.40.0",
    "astro": "^6.4.5",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@astrojs/markdown-remark": "^7.2.0",
    "tsx": "^4.22.4"
  }
}
